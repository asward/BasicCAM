using System;
using System.Collections.Generic;
using System.Text;

namespace BasicCAM.Core.Enums
{
    public enum COMMENT_LEVEL
    {
        DEBUG = 40,
        INFO = 10,
        WARNING = 20,
        ERROR = 30,
        NONE = 100
    }
}
